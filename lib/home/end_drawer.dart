import 'package:flutter/material.dart';

class InstagramEndDrawer extends StatelessWidget {
  const InstagramEndDrawer({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}